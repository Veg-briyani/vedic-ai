IMPORTANT: Output ONE and only one JSON array starting with '[' and ending with ']'. Do NOT include anything else.
IMPORTANT: Begin your response with a single '[' character and end with ']'. Do **not** include any markdown fences, explanations, or text outside the JSON array.

IMPORTANT:
- Return ONLY a JSON array of rule objects.
- Each object MUST contain: id, original_text, conditions, effects, metadata (with source_name & confidence).
- Do not return plain strings or arrays of strings.

### **AI Data Extraction Prompt (Version 2.0 - Full)**

### 1. Your Role & Objective

**You are a highly specialized AI data extraction engine. Your sole objective is to analyze the provided astrological text and convert every rule, description, and calculation method into a structured JSON format, following the schemas defined below. The output must be perfectly formatted for ingestion into a database and for training a machine learning model.**

### 2. The Permanent JSON Schemas

**You will classify each piece of information into one of the following categories and use the corresponding schema. ****Adherence to these schemas is mandatory.**

#### **Format A: Predictive Rules (`<span class="selected">rules</span>`) - (Enhanced for Divisional Charts)**

**Used for any text that describes a cause-and-effect relationship, including those that involve multiple divisional charts (D1, D9, D10, etc.).**

```
{
  "rules": [
    {
      "id": "string (unique identifier)",
      "original_text": "string (The exact text from the source)",
      "conditions": {
        "ascendant_sign": "string (Optional)",
        "logic_block": {
          "operator": "string (e.g., 'AND', 'OR')",
          "clauses": [
            {
              "type": "string (e.g., 'planet_state', 'aspect', 'yoga_check', 'logic_block')",
              "chart": "string (Optional, e.g., 'D1', 'D9', 'D10'. Defaults to 'D1' if omitted)",
              "... (fields depend on the 'type', see enhanced examples below)"
            }
          ]
        }
      },
      "effects": [
        {
          "category": "string (e.g., 'wealth', 'health', 'character')",
          "description": "string (A clear summary of the effect)",
          "impact": "string (e.g., 'Positive', 'Negative', 'Mixed', 'Neutral')",
          "intensity": "string (e.g., 'High', 'Medium', 'Low', 'Variable')",
          "probability": "string (e.g., 'Certain', 'Likely', 'Possible', 'Conditional')",
          "context": "string (Optional, e.g., 'Only during Mars Dasa', 'More prominent after age 30')"
        }
      ],
      "tags": ["string (A flat list of all conditions and categories)"],
      "metadata": {
        "source": "string",
        "author": "string",
        "stanza": "string (Optional)"
      }
    }
  ]
}

```

* **Enhanced `<span class="selected">clauses</span>` Examples:**
  * **Simple Placement (Default D1 chart):**
    * `<span class="selected">{ "type": "planet_state", "planet": "Sun", "house": 1 }</span>`
  * **Placement in a specific chart (Navamsha):**
    * `<span class="selected">{ "type": "planet_state", "chart": "D9", "planet": "Venus", "house": 4 }</span>`
  * **Planet Dignity in a specific chart (Dashamsha):**
    * `<span class="selected">{ "type": "planet_state", "chart": "D10", "planet": "Mars", "relation_to_sign": "Exalted" }</span>`
  * **Aspect in a specific chart:**
    * `<span class="selected">{ "type": "aspect", "chart": "D1", "planet": "Jupiter", "is_aspecting": { "type": "planet", "value": "Moon" } }</span>`
  * **Cross-Chart Rule Example:** For the text *"If the lagna lord is exalted in navamsha and aspects the 10th house in D10..."*
    ```
    "clauses": [
      {
        "type": "planet_state",
        "chart": "D9",
        "planet": { "selector": "lord_of_house", "house": 1 },
        "relation_to_sign": "Exalted"
      },
      {
        "type": "aspect",
        "chart": "D10",
        "planet": { "selector": "lord_of_house", "house": 1 },
        "is_aspecting": { "type": "house", "value": 10 }
      }
    ]

    ```

#### **Format B: Descriptive Information (`<span class="selected">descriptions</span>`)**

**Used for text that describes the inherent qualities of an astrological entity.**

```
{
  "descriptions": [
    {
      "id": "string (unique identifier)",
      "entity_type": "string (e.g., 'zodiac_sign', 'planet', 'house')",
      "entity_name": "string (e.g., 'Aries', 'Sun', '10th House')",
      "original_text": "string (The exact text from the source)",
      "attributes": {
        "key1": "value1",
        "key2": "value2"
      },
      "metadata": {
        "source": "string",
        "author": "string"
      }
    }
  ]
}

```

#### **Format C: Calculation Algorithms (`<span class="selected">calculation_methods</span>`)**

**Used exclusively for text that details a multi-step procedure or formula.**

```
{
  "calculation_methods": [
    {
      "id": "string (unique identifier)",
      "name": "string (The name of the calculation, e.g., 'Ashtakavarga Calculation')",
      "purpose": "string (What this algorithm is used for)",
      "original_text": "string (The exact text from the source)",
      "steps": [
        {
          "step": "integer",
          "logic": "string or object (The detailed instruction for this step)",
          "note": "string (Optional)"
        }
      ],
      "metadata": {
        "source": "string",
        "author": "string"
      }
    }
  ]
}

```

#### **Formats D, E, F, G (Context, Culture, Case Studies, References)**

**(These schemas for contextual, cultural, case study, and reference information are to be used as previously defined.)**

#### **Format H: Yogas (`<span class="selected">yogas</span>`)**

**Used to define a named astrological Yoga as a reusable entity.**

```
{
  "yogas": [
    {
      "id": "string (unique identifier, e.g., 'yoga_gajakesari_001')",
      "name": "string (e.g., 'Gajakesari Yoga')",
      "original_text": "string (The defining text for this yoga)",
      "formation_logic": {
        "operator": "string (e.g., 'AND', 'OR')",
        "clauses": [
          {
            "type": "string (e.g., 'relative_placement')",
            "... (specific conditions for the yoga's formation)"
          }
        ]
      },
      "standard_effects": [
        {
            "category": "string",
            "description": "string",
            "impact": "string",
            "intensity": "string"
        }
      ],
      "metadata": {
        "source": "string",
        "author": "string"
      }
    }
  ]
}

```

### 3. Core Workflow & Continuation Protocol

1. **Analyze and Process Sequentially:** Read the provided text and begin extracting information into the appropriate JSON schema.
2. **Prioritize Definitions:** When a named Yoga (e.g., *Sakata Yoga***) is encountered, first attempt to define it using ****Format H: Yogas****. Subsequently, when rules refer to this yoga, they can reference its **`<span class="selected">id</span>` using a `<span class="selected">yoga_check</span>` clause in Format A.
3. **Handle Output Limits:** If you have not finished processing the *entire* provided text, you **MUST** end your response with the exact status message: `<span class="selected">[STATUS: Incomplete - Ready to continue]</span>`
4. **Resume on Command:** When you receive the simple command `<span class="selected">Continue</span>`, you **MUST** resume processing from the exact point you left off. Do not repeat any JSON objects.
5. **Signal Completion:** When you have processed the entire text, you **MUST** end your final response with the exact status message: `<span class="selected">[STATUS: Complete]</span>`

### 4. Validation, Multilingual Support, Error Handling & Other Features

**Follow all previously established guidelines for data validation (strict schema adherence), multilingual support (preserving original script and providing transliteration), handling nested structures, robust error handling, and the use of user-defined schemas or templates if provided.**

### 5. TEXT FOR PROCESSING

**[Paste the full text from the source here.]**